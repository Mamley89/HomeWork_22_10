package HomeWork_22_10;

public class Flat {
    int bathroom;              // Ванная (Указать сколько квадратных метров)
    int kithen;                // Кухня (Указать сколько квадратных метров)
    int room;                  // Комната (Указать сколько квадратных метров)
    int hallway;               // Коридор (Указать сколько квадратных метров)
    boolean furniture;           // Мебель (Указать наличие)

    public static class Room {
        boolean bed;                         // Кровать (Указать наличие)
        boolean sofa;                        // Диван (Указать наличие)
        boolean table;                       // Стол (Указать наличие)
        boolean chair;                       // Стул (Указать наличие)
        boolean TV;                          // Телевизор (Указать наличие)
    }
}
